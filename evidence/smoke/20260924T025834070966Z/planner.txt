Validated plan saved to artifacts/plan.json.
{
  "strategy": "Prefer a clear goal direction first, otherwise use the first unblocked fallback direction, and STOP if no direction is clear.",
  "decisions": [
    {
      "when": "CLEAR_TARGET",
      "select": "TARGET"
    },
    {
      "when": "NO_CLEAR_TARGET",
      "select": "FIRST_CLEAR"
    }
  ],
  "fallback_order": [
    "FORWARD",
    "LEFT",
    "RIGHT"
  ],
  "stop_condition": "NO_CLEAR_EXIT"
}

