Validated plan saved to artifacts/plan.json.
{
  "strategy": "Prefer to move towards the goal direction if it is clear, otherwise choose the first unblocked fallback direction, and STOP if both are blocked.",
  "decisions": [
    {
      "when": "CLEAR_TARGET",
      "select": "TARGET"
    },
    {
      "when": "NO_CLEAR_TARGET",
      "select": "FIRST_CLEAR"
    }
  ],
  "fallback_order": [
    "FORWARD",
    "LEFT",
    "RIGHT"
  ],
  "stop_condition": "NO_CLEAR_EXIT"
}

